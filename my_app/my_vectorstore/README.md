This folder is empty and will be used to store your vectorstore data 
if using the default configs.

To change the folder, edit lifepim_config.py

OLD: `VECTOR_CACHE_DIR = REPO_ROOT / "my_app" / "my_vectorstore"` 

NEW: `VECTOR_CACHE_DIR = r"your folder"`

